#ifndef RATIO_INCLUDED
#define RATIO_INCLUDED

namespace core
{
	/// A simple struct to represent a ratio (fraction value) for both width and height
	typedef struct
	{
		double x;
		double y;
	} ratio;
}

#endif //RATIO_INCLUDED

