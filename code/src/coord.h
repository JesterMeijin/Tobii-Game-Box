#ifndef COORD_INCLUDED
#define COORD_INCLUDED

namespace core
{
	/// This structure store a pair of coordinates value (to help in the rest of the program)
	typedef struct
	{
		int x;
		int y;
	} coord;
}

#endif //COORD_INCLUDED
